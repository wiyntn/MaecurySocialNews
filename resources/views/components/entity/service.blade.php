@props(['service'])

<div class="w-full flex-center h-40 bg-fill-qt relative">
	<div class="size-12">
		<img src="{{ asset('assets/icons/services/'.$service.'.png') }}" alt="{{ $service }}" class="w-full">
	</div>
</div>
